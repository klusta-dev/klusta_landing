# Static assets

Place logos and other images here, e.g.:

- `logo.svg` or `logo.png` – main logo
- `logo-dark.svg` – dark variant
- `favicon.ico` – favicon

Reference in code:

- From root: `/assets/logo.svg`
- In Next.js: `<Image src="/assets/logo.svg" alt="Klusta" width={120} height={32} />`
